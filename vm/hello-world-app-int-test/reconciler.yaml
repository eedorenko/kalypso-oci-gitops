---
- hosts: localhost
  tasks:
  - name: install cron job (ansible-pull)
    cron:
      name: "ansible-pull-dsc"
      minute: "*/1"
      job: |
        ansible-pull -o -U https://github.com/eedorenko/kalypso-oci-app-gitops -C dev int-test/install.yml > /tmp/ansible-pull.log 2>&1

